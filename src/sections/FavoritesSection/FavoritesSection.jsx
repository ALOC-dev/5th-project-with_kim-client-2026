import ListingCard from '../../components/ListingCard';
import './FavoritesSection.css';

export default function FavoritesSection({ listings, favorites, compareIds, onSelect, onFavorite, onCompare, registryUploads, onUploadRegistry }) {
  const favoriteListings = listings.filter((listing) => favorites.includes(listing.id));
  return <section className="content-section favorites-section"><header><div><h1>찜한 매물</h1><p>나중에 비교할 매물을 모아 두었어요.</p></div><b>{favoriteListings.length}개</b></header>{favoriteListings.length ? <><div className="favorites-section__notice">최대 3개까지 선택해 주요 조건을 한눈에 비교할 수 있어요.</div><div className="favorites-section__list">{favoriteListings.map((listing) => <ListingCard key={listing.id} listing={listing} isFavorite onSelect={onSelect} onFavorite={onFavorite} compareSelected={compareIds.includes(listing.id)} onCompare={onCompare} registryUpload={registryUploads[listing.id]} onUploadRegistry={onUploadRegistry} />)}</div>{compareIds.length > 1 && <Comparison listings={favoriteListings.filter((listing) => compareIds.includes(listing.id))} />}</> : <div className="favorites-section__empty"><strong>아직 찜한 매물이 없어요.</strong><span>마음에 드는 매물의 하트를 눌러 보관해 보세요.</span></div>}</section>;
}
function Comparison({ listings }) { return <section className="comparison"><h2>선택 매물 비교</h2><div>{['가격', '안전 점수', '학교까지', '리뷰 평점'].map((label) => <div className="comparison__row" key={label}><b>{label}</b>{listings.map((listing) => <span key={listing.id}>{label === '가격' ? (listing.rent ? `${listing.deposit}/${listing.rent}` : listing.deposit) : label === '안전 점수' ? listing.safetyScore : label === '학교까지' ? `도보 ${listing.walkingMinutes}분` : listing.rating}</span>)}</div>)}</div></section>; }
